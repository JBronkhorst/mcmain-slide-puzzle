import { PuzzleSolvedPipe } from './puzzle-solved-pipe';

describe('PuzzleSolvedPipe', () => {
  it('create an instance', () => {
    const pipe = new PuzzleSolvedPipe();
    expect(pipe).toBeTruthy();
  });
});
